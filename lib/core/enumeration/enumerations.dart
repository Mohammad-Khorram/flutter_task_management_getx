enum ESnackBarIconType { info, exception, connection }

enum EHttpStatusCode {
  status400BadRequest,
  status401Unauthorized,
  status403Forbidden,
  status404NotFound,
  status405MethodNotAllowed,
  status500InternalServerError,
}

enum EFilterType { all, completed, incomplete }

enum EPaginationType { first, pagination }
